# Default: LF for all text files
* text=auto eol=lf

# Force CRLF for Windows-specific files
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary files (prevent mangling)
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.whl binary
*.jar binary
